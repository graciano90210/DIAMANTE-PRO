name: diamante_pro
description: Aplicación móvil para el sistema Diamante PRO
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Manejo de estado
  provider: ^6.1.1
  
  # Peticiones HTTP
  http: ^1.1.2
  
  # Almacenamiento local
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0
  path_provider: ^2.1.1
  path: ^1.8.3
  
  # Conectividad
  connectivity_plus: ^5.0.2
  
  # Captura de imágenes
  image_picker: ^1.0.7
  
  # Geolocalización
  geolocator: ^11.0.0
  permission_handler: ^11.2.0
  
  # Enlaces externos (WhatsApp, llamadas, mapas)
  url_launcher: ^6.2.4
  
  # Iconos
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^10.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
