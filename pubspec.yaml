name: diamante_pro
description: Aplicación móvil para el sistema Diamante PRO
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Manejo de estado
  provider: ^6.1.1
  
  # Peticiones HTTP
  http: ^1.1.2
  
  # Almacenamiento local
  shared_preferences: ^2.2.2
  
  # Iconos
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  # assets:
  #   - images/
  #   - assets/
